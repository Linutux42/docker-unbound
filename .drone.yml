---
kind: pipeline
type: docker
name: default-arm64

platform:
  os: linux
  arch: arm64

steps:
- name: get-version
  image: debian:buster-slim
  commands:
  - "UNBOUND=$(apt-cache policy | sed -n -e 's/^.*Candidate: //p' | cut -d'-' -f1)"
  - "echo ${UNBOUND},latest > .tags"
- name: build
  image: plugins/docker
  settings:
    dockerfile: Dockerfile
    repo: linutux/unbound
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password

trigger:
  branch:
  - master
  event:
  - push
---
kind: signature
hmac: 5a02a0be02176f675588c9124f93eb3b288dc6bf17829e6c273a7d173a497a3a

...
