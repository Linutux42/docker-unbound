---
kind: pipeline
type: docker
name: default-arm64

platform:
  os: linux
  arch: arm64

steps:
- name: get-version
  image: debian:buster-slim
  commands:
  - "apt-get update"
  - "APPVERSION=$(apt-cache policy | sed -n -e 's/^.*Candidate: //p' | cut -d'-' -f1 | tr -d '\n')"
  - "echo $APPVERSION,latest > .tags"
- name: build
  image: plugins/docker
  settings:
    dockerfile: Dockerfile
    repo: linutux/unbound
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password

trigger:
  branch:
  - master
  event:
  - push
---
kind: signature
hmac: 4898e0f0adddaaf31f4aa335a300fc78b51a57e45b6d381d84ead94813b1aa06

...
